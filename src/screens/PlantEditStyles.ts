import {StyleSheet} from 'react-native';

export const PlantEditStyles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 12,
    paddingBottom: 12,
    flexDirection: 'column',
  },
  header: {
    paddingTop: 16,
    alignSelf: 'center',
    fontSize: 24,
  },
});
