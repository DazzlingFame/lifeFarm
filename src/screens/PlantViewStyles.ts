import {StyleSheet} from 'react-native';

export const PlantViewStyles = StyleSheet.create({
  headerTextContainer: {flexDirection: 'row', alignSelf: 'center'},
  headerText: {
    fontSize: 20,
  },
});
