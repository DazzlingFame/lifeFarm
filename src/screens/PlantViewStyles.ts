import {StyleSheet} from 'react-native';

export const PlantViewStyles = StyleSheet.create({
  headerTextContainer: {
    flexDirection: 'row',
  },
  headerText: {
    textAlign: 'center',
    fontSize: 20,
  },
});
